{
  "name": "test-config",
  "version": "1.0.0",
  "rules": {
    "no-console": "warn",
    "quotes": ["error", "single"]
  },
  "settings": {
    "enabled": true,
    "timeout": 5000,
    "features": ["a", "b", "c"]
  }
} 